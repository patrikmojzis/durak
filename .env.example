# Public URL used when the API returns browser and agent links.
# For production, set this to your HTTPS subdomain.
PUBLIC_BASE_URL=https://durak.example.com

# Bind only to localhost when running behind Caddy/Nginx on the same server.
HOST_BIND=127.0.0.1
HOST_PORT=8000
